<%= render 'events/events', title: 'Evenement qui est passée', events: @events_depassed %>
<%= render 'events/events', title: "Evenement d'actualitée",events: @events_current %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_event_path,
            :class => 'btn btn-primary' %>
